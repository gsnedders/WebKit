Referrer Policy: iframes srcdoc correctly inherit the ancestor's referrer policy



PASS Srcdoc iframe inherits referrer policy from parent on creation.
PASS Even after a navigation from another initiator, srcdoc iframe still inherits referrer policy from the parent.

