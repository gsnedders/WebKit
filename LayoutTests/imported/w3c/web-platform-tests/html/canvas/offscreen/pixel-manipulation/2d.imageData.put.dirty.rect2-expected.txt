2d.imageData.put.dirty.rect2

putImageData() only modifies areas inside the dirty rectangle, using x and y


FAIL putImageData() only modifies areas inside the dirty rectangle, using x and y Can't find variable: OffscreenCanvas

