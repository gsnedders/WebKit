2d.imageData.put.dirty.outside

putImageData() handles dirty rectangles outside the canvas correctly


FAIL putImageData() handles dirty rectangles outside the canvas correctly Can't find variable: OffscreenCanvas

