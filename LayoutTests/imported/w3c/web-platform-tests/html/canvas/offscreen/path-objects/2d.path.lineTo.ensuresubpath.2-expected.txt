2d.path.lineTo.ensuresubpath.2

If there is no subpath, the point is added and used for subsequent drawing


FAIL If there is no subpath, the point is added and used for subsequent drawing Can't find variable: OffscreenCanvas

