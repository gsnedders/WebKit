2d.path.stroke.prune.rect

Zero-length line segments from rect and strokeRect are removed before stroking


FAIL Zero-length line segments from rect and strokeRect are removed before stroking Can't find variable: OffscreenCanvas

