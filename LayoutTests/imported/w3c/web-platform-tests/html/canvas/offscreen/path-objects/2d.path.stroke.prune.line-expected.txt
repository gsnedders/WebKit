2d.path.stroke.prune.line

Zero-length line segments from lineTo are removed before stroking


FAIL Zero-length line segments from lineTo are removed before stroking Can't find variable: OffscreenCanvas

