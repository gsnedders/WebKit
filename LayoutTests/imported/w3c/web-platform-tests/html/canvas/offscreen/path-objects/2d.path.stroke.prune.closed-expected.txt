2d.path.stroke.prune.closed

Zero-length line segments from closed paths are removed before stroking


FAIL Zero-length line segments from closed paths are removed before stroking Can't find variable: OffscreenCanvas

