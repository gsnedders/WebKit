2d.path.stroke.prune.corner

Zero-length line segments are removed before stroking with miters


FAIL Zero-length line segments are removed before stroking with miters Can't find variable: OffscreenCanvas

