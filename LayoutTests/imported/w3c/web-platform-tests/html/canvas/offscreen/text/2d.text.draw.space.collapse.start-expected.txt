2d.text.draw.space.collapse.start

Space characters at the start of a line are collapsed (per CSS)


FAIL Space characters at the start of a line are collapsed (per CSS) Can't find variable: OffscreenCanvas

