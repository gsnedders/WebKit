2d.shadow.outside

Shadows of shapes outside the visible area can be offset onto the visible area


FAIL Shadows of shapes outside the visible area can be offset onto the visible area Can't find variable: OffscreenCanvas

