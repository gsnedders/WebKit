2d.shadow.gradient.alpha

Shadows are drawn correctly for partially-transparent gradient fills


FAIL Shadows are drawn correctly for partially-transparent gradient fills Can't find variable: OffscreenCanvas

